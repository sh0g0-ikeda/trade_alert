# フロントエンド進捗レポート

## 2026-01-14

### 完了
- APIクライアントに `X-API-Key` を付与し、プッシュトークン登録のパスを `/push-tokens` に統一。
- 認証フロー（ログイン/新規登録/トークン永続化）を整理し、画面の文字化け・構文エラーを修正。
- ホーム画面（アラート作成/一覧/有効切替/削除）を再構成し、通知トークン登録の状態表示を実装。
- 通知履歴タブを追加し、`/alerts/history` と `/alerts/stats/summary` の表示を実装。
- ポートフォリオ画面を整理し、追加/編集/削除の導線を実装。
- 設定タブを追加し、アカウント情報とプラン情報の表示を実装。
- 静的チェックの警告（未使用変数）を解消。
- ポートフォリオ画面の未使用API呼び出しを整理。
- lint警告（未使用import）を解消。

### 未完/保留
- スクショ一括インポートUI（バックエンドは準備済みのため後続対応）。
- プラン詳細・アップグレード画面。

### テスト
- 未実行（ローカル検証・lint は次回実施予定）。
